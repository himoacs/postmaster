# Prevent pnpm from caching native module builds across architectures
# This fixes recurring better-sqlite3 x86_64/arm64 architecture mismatches
side-effects-cache=false

# Force rebuild of native modules on install  
# This ensures better-sqlite3 is always compiled for the current architecture
prefer-built=false
